gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
# Note: We intentionally track the app keystore to ensure consistent APK signing
# This allows APK upgrades without requiring full uninstall/reinstall
# The keystore is for an open-source app with publicly available credentials
!app/keystore/agixt-keystore.jks
